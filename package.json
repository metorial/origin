{
  "name": "@metorial/origin-root",
  "private": true,
  "scripts": {
    "dev": "docker compose -p origin -f ./docker-compose.dev.yml up --pull always --build code-bucket"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "turbo": "^2.5.5",
    "typescript": "5.8.2"
  },
  "engines": {
    "node": ">=18",
    "bun": ">=1.2.15"
  },
  "packageManager": "bun@1.2.15",
  "workspaces": [
    "apps/*",
    "packages/*",
    "clients/*"
  ],
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}